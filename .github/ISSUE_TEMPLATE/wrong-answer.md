---
name: Неверный ответ
about: Модель или инструмент ответили не то, поиск не нашёл нужный метод, поле или объект
title: 'Неверный ответ: '
labels: bug
---

<!--
Не вставляйте сюда пароли базы данных и MCP_AUTH_TOKEN: репозиторий публичный.
Не вставляйте строки данных из базы и код, который нельзя показывать посторонним.
-->

## Что спросили

Вопрос к модели — дословно или своими словами:

## Какой инструмент вызван

Вызовы видно на дашборде: **Сессии** → нужная сессия → вызов, панели
**Input** и **Ответ**.

- Инструмент:
- Вход:

```
```

## Что вернулось

Главное из ответа инструмента или модели, достаточно первых строк:

```
```

## Какой ответ правильный

Метод, функция, поле или объект, которые должны были найтись, либо верное
описание:

## Окружение

Вывод `curl http://localhost:3100/readyz` — в нём версия сервера, версия HCM
(`hcm_version`) и режим поиска (`search_mode`):

```
```

- LLM-клиент и модель:
- Правила из `prompts/` подключены: да / нет
