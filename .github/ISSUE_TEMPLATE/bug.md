---
name: Ошибка
about: Сервер не запускается или работает не так, как написано в документации
title: ''
labels: bug
---

<!--
Не вставляйте сюда пароли базы данных и MCP_AUTH_TOKEN: репозиторий публичный.
В логах пароли не печатаются, но перед отправкой просмотрите их.
-->

## Что произошло

## Что ожидали увидеть

## Как повторить

1.
2.
3.

## Версия сервера

Вывод `curl http://localhost:3100/healthz`:

```
```

## Состояние

Вывод `curl http://localhost:3100/readyz` (или сообщение об ошибке):

```
```

Вывод `docker compose ps`:

```
```

## Окружение

- ОС машины с Docker: Windows / Linux / macOS
- Docker: Desktop / Engine, версия
- СУБД: MS SQL / PostgreSQL, версия
- Каталог WebSoftServer: локальная папка / сетевая шара / копия
- LLM-клиент (если ошибка в нём):

## Логи

Приложите `logs/startup-<дата>.log` и `logs/errors-<дата>.log` за день, когда
воспроизвелась ошибка. Если контейнер перезапускается — ещё
`docker compose logs --tail 100`.
